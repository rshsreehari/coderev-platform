# ═══════════════════════════════════════════════════════
# CODE REVIEW PLATFORM — Environment Variables
# ═══════════════════════════════════════════════════════
#   For local dev:   cp .env.example .env.local
#   For production:  cp .env.example .env.production
# ═══════════════════════════════════════════════════════

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=codereviews
DB_USER=postgres
DB_PASSWORD=your_db_password_here

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# SQS
AWS_REGION=us-west-2
SQS_QUEUE_NAME=code-review-jobs
SQS_DLQ_NAME=code-review-jobs-dlq
SQS_VISIBILITY_TIMEOUT=30
SQS_MAX_RECEIVE_COUNT=3

# For local development with LocalStack:
# AWS_ENDPOINT=http://localhost:4566
# AWS_ACCESS_KEY_ID=test
# AWS_SECRET_ACCESS_KEY=test

# Server
PORT=3000
NODE_ENV=development

# Gemini AI Configuration
AI_PROVIDER=gemini
AI_MODEL=gemini-2.0-flash
AI_API_KEY=gemini_api_key_here
ENABLE_AI=true

# EC2 Public IP (production only — for CORS)
# EC2_PUBLIC_IP=your_ec2_ip_here
